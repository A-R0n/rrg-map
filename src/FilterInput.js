import React from "react";

export const FilterInput = () => {
    const name = "";
    console.log("props: " + name);
    return (
        <div className="filterInput">
            {"props is undefined"}
        </div>
    )
}