import React from "react";
// import { routeData } from "./data/routeData";

export const Routes = () => {
    return (
        <div className="route-container">
            The route list after filtering needs to be mapped
        </div>
    )
}